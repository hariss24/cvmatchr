# Journal — Arbitre du 02/08/2026

## Contexte

Premier réveil du rôle Arbitre depuis le changement de mandat de la boucle (« la
boucle propose et classe, elle n'implémente plus », commit `ef5fa2a`). `IDEES.md`
était vide (« le premier Arbitre le remplira à partir de `boucle/BACKLOG.md` et des
constats »). Ce réveil remplit ce premier classement.

## Ce qui a été lu

`MISSION.md`, `ETAT.md`, `IDEES.md`, `roles/arbitre.md`, les trois constats de
`boucle/constats/` (manques fonctionnels 01/08, performance 31/07, état CI 31/07) et
`BACKLOG.md` en entier (pas seulement `## À planifier` — nécessaire pour amorcer un
classement vide, comme le prévoit le commentaire de `IDEES.md` ligne 33 : « le
premier Arbitre le remplira à partir de `boucle/BACKLOG.md` et des constats », sans
restriction de section). Les Arbitres suivants n'auront plus besoin de relire tout
`BACKLOG.md` : chaque idée passée dans `IDEES.md` n'a plus à y être relue.

Deux vérifications ponctuelles avant de classer, pour ne pas noter une idée déjà
close comme si elle restait ouverte :
- Le constat CI du 31/07 signalait `tsc --noEmit` cassé sur
  `useAutoDraft.test.ts` (mock `{ name, title }` ne satisfaisant plus `DocData`).
  Lecture directe du fichier : il utilise désormais `{ ...DEFAULT_RESUME, name,
  title }` — corrigé, très probablement en effet de bord de la Task 3 du plan
  zod-global (« 7 fichiers de test restants migrés », journal Bâtisseur du 01/08).
  Aucune commande `tsc` n'a pu être exécutée dans cet environnement
  (`node_modules` absent, hors périmètre de réinstaller pour un rôle qui n'écrit
  pas de code) — la vérification s'appuie sur la lecture du fichier, pas sur une
  exécution. Non repris au classement : plus une idée à trancher.
- Le chantier n°3 du constat performance du 31/07 (« pourquoi le chunk zod
  référencé sur `/pack` ne s'est pas chargé ») est résolu par la mesure finale du
  plan zod-global déjà terminé (`BACKLOG.md` § Terminé) : `/pack` a bien perdu ce
  chunk. Non repris non plus.

## Ce qui a été classé

11 idées, tirées de `## À planifier` (7 lignes) et `## Idées` (3 lignes non
écartées) de `BACKLOG.md`. Non reprises : l'extension navigateur §1 du constat
manques-fonctionnels (déjà construite, en attente de fusion — plus une idée) et les
deux lignes déjà écartées par le propriétaire (mock interview, CRM), recopiées
telles quelles sans note, comme l'exige `roles/arbitre.md`.

Ordre retenu (total sur 20, détail des 4 notes dans `IDEES.md`) :

1. Optimisation de profil LinkedIn — 14 (spec+plan déjà prêts, `## Prêt à coder`)
2. Autofill sur d'autres ATS — 13 (*Facilité peu fiable*)
3. Skill Gap Analyzer — 12 (*Facilité peu fiable*)
4. Performance `/jobs` (remesure) — 12
5. Alléger `/` (éditeur) — 11
6. Robustesse du scan — 11
7. Performance `/pack` (remesure) — 11
8. Journal de candidature — 10
9. Import direct profil LinkedIn — 8
10. Capture d'offre native (extension) — 8 (*Facilité peu fiable*)
11. Générateur de lettre de démission — 8

## Décisions de tranchage

- **Journal de candidature (10/20)** partage la même tension structurelle que la
  ligne « CRM de networking » déjà écartée par le propriétaire (contre-courant du
  principe « zéro coût pour l'utilisateur » du tracker actuel) : je l'ai signalé
  explicitement dans sa justification plutôt que de la noter en silence, pour que
  le propriétaire tranche en connaissance de cause. Je ne l'ai pas classée dans
  « Écartées » moi-même — ce n'est pas la même idée, et seul le propriétaire écarte.
- Trois égalités de total ont été départagées par Cohérence puis Apport, comme le
  prévoit `roles/arbitre.md` : à 12/20, Skill Gap Analyzer (Cohérence 4) passe
  devant Performance `/jobs` (Cohérence 3) ; à 11/20, Alléger `/` (Cohérence 4)
  passe devant Robustesse du scan et Performance `/pack` (Cohérence 3 toutes les
  deux, départagées à leur tour par Apport : 3 contre 2) ; à 8/20, Import LinkedIn
  (Cohérence 3) passe devant Capture d'offre (Cohérence 2) puis Lettre de
  démission (Cohérence 1).
- Les deux remesures de performance (`/jobs`, `/pack`) sont classées comme des
  idées à part entière bien qu'il s'agisse de mesurer plutôt que construire :
  `MISSION.md` fixe des seuils vérifiables des deux côtés (fonctionnel et
  technique) et la règle « un manque fonctionnel prime sur un dépassement
  technique sauf facteur 2 » ne s'applique qu'une fois le dépassement confirmé —
  ces deux lignes existent justement pour lever cette incertitude. Leur Écart
  concurrence est noté à 1 par construction (seuil interne à `MISSION.md`, pas une
  comparaison à la concurrence) plutôt que laissé sans note, conformément à la
  règle « aucune note sans phrase ».

## Ce qui reste inchangé

`## Écartées` recopié à l'identique (mock interview, CRM de networking) — aucun
arbitrage du propriétaire retouché.
